import * as React from 'react';
import { Tooltip, Box } from '@chakra-ui/react';

export interface IAppProps {
}

export default function LandingAuth (props: IAppProps) {
  return (
    <Box>
      <Tooltip>
          Come in
      </Tooltip>
    </Box>
  );
}
